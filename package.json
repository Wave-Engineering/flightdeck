{
  "name": "flightdeck",
  "version": "0.2.9",
  "private": true,
  "description": "FlightDeck — event-sourced status console for wave campaigns & lazyriver floats (container service).",
  "type": "module",
  "module": "src/server.ts",
  "scripts": {
    "start": "bun run src/server.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
